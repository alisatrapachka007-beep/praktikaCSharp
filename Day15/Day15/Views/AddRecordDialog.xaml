<Window x:Class="Day15.Views.AddRecordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Медицинская запись" Height="400" Width="500"
        WindowStartupLocation="CenterOwner">
	<StackPanel Margin="20">
		<TextBlock FontWeight="Bold" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,15">
			Медицинская запись
		</TextBlock>

		<TextBlock Text="Пациент:" FontWeight="Bold" Margin="0,0,0,5"/>
		<TextBlock x:Name="PatientNameText" Margin="0,0,0,15" FontWeight="SemiBold"/>

		<TextBlock Text="Дата приема:" FontWeight="Bold" Margin="0,0,0,5"/>
		<DatePicker x:Name="DatePicker" Margin="0,0,0,15"/>

		<TextBlock Text="Диагноз:" FontWeight="Bold" Margin="0,0,0,5"/>
		<TextBox x:Name="DiagnosisTextBox" Margin="0,0,0,15"/>

		<TextBlock Text="Заметки врача:" FontWeight="Bold" Margin="0,0,0,5"/>
		<TextBox x:Name="NotesTextBox" Height="80" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,20"/>

		<StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
			<Button Content="Сохранить" Width="100" Click="SaveButton_Click" Margin="0,0,10,0"/>
			<Button Content="Отмена" Width="80" Click="CancelButton_Click"/>
		</StackPanel>
	</StackPanel>
</Window>